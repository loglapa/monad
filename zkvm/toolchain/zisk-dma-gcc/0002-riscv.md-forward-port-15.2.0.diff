--- a/gcc/config/riscv/riscv.md
+++ b/gcc/config/riscv/riscv.md
@@ -2755,8 +2755,14 @@
                            (match_operand:BLK 2)))
 	      (use (match_operand:SI 3))
 	      (use (match_operand:SI 4))])]
-  "!optimize_size"
+  "!optimize_size || TARGET_ZISK_DMA"
 {
+  /* ZisK first: the DMA marker is one insn, so it wins at any -O level.  */
+  if (TARGET_ZISK_DMA
+      && riscv_zisk_expand_cmpmem (operands[0], operands[1], operands[2],
+				   operands[3]))
+    DONE;
+
   /* If TARGET_VECTOR is false, this routine will return false and we will
      try scalar expansion.  */
   if (riscv_vector::expand_vec_cmpmem (operands[0], operands[1],
@@ -2797,48 +2803,10 @@
 
 ;; memmove.  The ZisK memcpy precompile honours overlapping ranges, so it
 ;; satisfies the memmove contract and the same insns serve both.
-(define_expand "movmem<mode>"
-  [(parallel [(set (match_operand:BLK 0 "general_operand")
-		   (match_operand:BLK 1 "general_operand"))
-	      (use (match_operand:P 2 ""))
-	      (use (match_operand:SI 3 "const_int_operand"))])]
-  "TARGET_ZISK_DMA"
-{
-  if (riscv_zisk_expand_cpymem (operands[0], operands[1], operands[2]))
-    DONE;
-  else
-    FAIL;
-})
 
 ;; The marker pair, as one insn so it can never be split or scheduled apart.
 ;; Length in the addi immediate: one zisk instruction.
-(define_expand "setmem<mode>"
-  [(parallel [(set (match_operand:BLK 0 "memory_operand")
-		   (match_operand:QI 2 "nonmemory_operand"))
-	      (use (match_operand:P 1 ""))
-	      (use (match_operand:SI 3 "const_int_operand"))])]
-  "TARGET_ZISK_DMA"
-{
-  if (riscv_zisk_expand_setmem (operands[0], operands[1], operands[2]))
-    DONE;
-  else
-    FAIL;
-})
 
-(define_expand "cmpmemsi"
-  [(set (match_operand:SI 0 "register_operand")
-	(compare:SI (match_operand:BLK 1 "memory_operand")
-		    (match_operand:BLK 2 "memory_operand")))
-   (use (match_operand:SI 3 "general_operand"))
-   (use (match_operand:SI 4 "const_int_operand"))]
-  "TARGET_ZISK_DMA"
-{
-  if (riscv_zisk_expand_cmpmem (operands[0], operands[1], operands[2],
-				operands[3]))
-    DONE;
-  else
-    FAIL;
-})
 
 ;; memset, count and fill both immediate.
 (define_insn "riscv_zisk_setmem_imm<mode>"
@@ -2921,6 +2889,11 @@
 	      (use (match_operand:SI 3 "const_int_operand"))])]
  ""
 {
+  /* ZisK first: it takes a non-zero fill byte, which the paths below cannot.  */
+  if (TARGET_ZISK_DMA
+      && riscv_zisk_expand_setmem (operands[0], operands[1], operands[2]))
+    DONE;
+
   /* If TARGET_VECTOR is false, this routine will return false and we will
      try scalar expansion.  */
   if (riscv_vector::expand_vec_setmem (operands[0], operands[1], operands[2]))
@@ -2939,11 +2912,21 @@
 (define_expand "movmem<mode>"
   [(parallel [(set (match_operand:BLK 0 "general_operand")
    (match_operand:BLK 1 "general_operand"))
-    (use (match_operand:P 2 "const_int_operand"))
+    (use (match_operand:P 2 ""))
     (use (match_operand:SI 3 "const_int_operand"))])]
-  "TARGET_VECTOR"
+  "TARGET_VECTOR || TARGET_ZISK_DMA"
 {
-  if (riscv_vector::expand_block_move (operands[0], operands[1], operands[2],
+  /* The ZisK memcpy precompile honours overlapping ranges, so it satisfies the
+     memmove contract and the cpymem insns serve both.  The length predicate is
+     relaxed from const_int because the marker takes a register count too; the
+     vector path below still sees only what it asked for, since it runs only
+     under TARGET_VECTOR.  */
+  if (TARGET_ZISK_DMA
+      && riscv_zisk_expand_cpymem (operands[0], operands[1], operands[2]))
+    DONE;
+
+  if (TARGET_VECTOR
+      && riscv_vector::expand_block_move (operands[0], operands[1], operands[2],
 				       true))
     DONE;
   else
